#include <stdio.h>

int main (void) {
    printf("Hello. Welcome and Please accept our terms and policy... \n ");


    /* designate variables int*/
    int battery = 12;
    int lukewarm = 48;
    int median;
    int medallion;
    
    printf("The integer for a cup of lukewarm water is, %d \n" , lukewarm);

    printf("The integer for our engine battery is, %d \n", battery);
        
             /* statement that designates multiplier*/
            /* print integer for medallion output*/

        medallion = battery * lukewarm;

        printf("if my morning tea got onto the battery it would be!");
         printf("overload of, %d", medallion);


}